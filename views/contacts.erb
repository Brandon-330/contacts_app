<h1>
  Your Contacts
</h1>

<ul>
  <% @contacts.each do |contact| %>
    <li>
      <a href="/contacts/<%= contact[:id] %>">
        <h2><%= contact[:name] %></h2>
      </a>
      <p><%= contact[:category] %></p>
      <p><%= contact[:description] %></p>
    </li>
  <% end %>
</ul>