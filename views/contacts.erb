<h1>
  Your Contacts
</h1>

<ul>
  <% @contacts.each do |contact| %>
    <li>
      <a href="/contacts/<%= contact[:id] %>">
        <h2><%= contact[:name] %></h2>
      </a>
    </li>
  <% end %>
</ul>